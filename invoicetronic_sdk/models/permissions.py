# coding: utf-8

"""
    Invoicetronic API

    The [Invoicetronic API][2] is a RESTful service that allows you to send and receive invoices through the Italian [Servizio di Interscambio (SDI)][1], or Interchange Service. The API is designed to be simple and easy to use, abstracting away SDI complexity while providing complete control over the invoice send/receive process. It provides advanced features as encryption at rest, multi-language pre-flight invoice validation, multiple upload formats, webhooks, event logging, client SDKs, and CLI tools.  For more information, see  [Invoicetronic website][2]  ## Before you start  For the full integration guide, tutorials, SDKs and quickstarts, see the **[Documentation](https://invoicetronic.com/en/docs/)**. A few cross-cutting topics worth knowing before integrating:  - **[Prerequisites](https://invoicetronic.com/en/docs/prerequisites/)** — what you need to start in Sandbox and what's required to move to production. - **[API Keys](https://invoicetronic.com/en/docs/apikeys/)** — how `ik_live_` and `ik_test_` keys select the environment. - **[Sandbox](https://invoicetronic.com/en/docs/sandbox/)** — free test environment that mirrors the live workflow, with no credits consumed. - **[Rate Limiting](https://invoicetronic.com/en/docs/ratelimiting/)** — per-second, per-minute and per-day limits; how to handle `429 Too Many Requests`. - **[Pagination](https://invoicetronic.com/en/docs/pagination/)** — `page` and `page_size` parameters and the `Invoicetronic-Total-Count` response header. - **[CORS](https://invoicetronic.com/en/docs/cors/)** — calling the API from the browser; allowed origins are configured per key. - **[Webhooks](https://invoicetronic.com/en/docs/webhooks/)** — real-time event notifications with HMAC-SHA256 signature validation. - **[Localization](https://invoicetronic.com/en/docs/accept-language/)** — use the `Accept-Language` header to receive error messages in Italian, English or German.  [1]: https://www.fatturapa.gov.it/it/sistemainterscambio/cose-il-sdi/ [2]: https://invoicetronic.com/ 

    The version of the OpenAPI document: 1.17.0
    Contact: info@invoicetronic.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class Permissions(BaseModel):
    """
    Permissions of a restricted key, per resource. A missing property means no access to that resource. Each permission cannot exceed the one of the main key the restricted key belongs to.
    """ # noqa: E501
    company: Optional[StrictStr] = Field(default=None, description="Companies: `Read` lists and reads them, `Write` also creates, updates and deletes them.")
    send: Optional[StrictStr] = Field(default=None, description="Outgoing invoices: `Read` lists and reads them, `Write` also sends and validates invoices.")
    receive: Optional[StrictStr] = Field(default=None, description="Incoming invoices: `Read` lists and reads them, `Write` also deletes them.")
    webhook: Optional[StrictStr] = Field(default=None, description="Webhooks: `Read` lists and reads them, `Write` also creates, updates and deletes them.")
    update: Optional[StrictStr] = Field(default=None, description="SDI status updates of outgoing invoices.")
    log: Optional[StrictStr] = Field(default=None, description="Event log.")
    webhookhistory: Optional[StrictStr] = Field(default=None, description="Webhook delivery history.")
    export: Optional[StrictStr] = Field(default=None, description="Invoice export.")
    status: Optional[StrictStr] = Field(default=None, description="Account status (remaining operations and signatures).")
    __properties: ClassVar[List[str]] = ["company", "send", "receive", "webhook", "update", "log", "webhookhistory", "export", "status"]

    @field_validator('company')
    def company_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read', 'Write']):
            raise ValueError("must be one of enum values ('Read', 'Write')")
        return value

    @field_validator('send')
    def send_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read', 'Write']):
            raise ValueError("must be one of enum values ('Read', 'Write')")
        return value

    @field_validator('receive')
    def receive_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read', 'Write']):
            raise ValueError("must be one of enum values ('Read', 'Write')")
        return value

    @field_validator('webhook')
    def webhook_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read', 'Write']):
            raise ValueError("must be one of enum values ('Read', 'Write')")
        return value

    @field_validator('update')
    def update_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read']):
            raise ValueError("must be one of enum values ('Read')")
        return value

    @field_validator('log')
    def log_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read']):
            raise ValueError("must be one of enum values ('Read')")
        return value

    @field_validator('webhookhistory')
    def webhookhistory_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read']):
            raise ValueError("must be one of enum values ('Read')")
        return value

    @field_validator('export')
    def export_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read']):
            raise ValueError("must be one of enum values ('Read')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Read']):
            raise ValueError("must be one of enum values ('Read')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Permissions from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if company (nullable) is None
        # and model_fields_set contains the field
        if self.company is None and "company" in self.model_fields_set:
            _dict['company'] = None

        # set to None if send (nullable) is None
        # and model_fields_set contains the field
        if self.send is None and "send" in self.model_fields_set:
            _dict['send'] = None

        # set to None if receive (nullable) is None
        # and model_fields_set contains the field
        if self.receive is None and "receive" in self.model_fields_set:
            _dict['receive'] = None

        # set to None if webhook (nullable) is None
        # and model_fields_set contains the field
        if self.webhook is None and "webhook" in self.model_fields_set:
            _dict['webhook'] = None

        # set to None if update (nullable) is None
        # and model_fields_set contains the field
        if self.update is None and "update" in self.model_fields_set:
            _dict['update'] = None

        # set to None if log (nullable) is None
        # and model_fields_set contains the field
        if self.log is None and "log" in self.model_fields_set:
            _dict['log'] = None

        # set to None if webhookhistory (nullable) is None
        # and model_fields_set contains the field
        if self.webhookhistory is None and "webhookhistory" in self.model_fields_set:
            _dict['webhookhistory'] = None

        # set to None if export (nullable) is None
        # and model_fields_set contains the field
        if self.export is None and "export" in self.model_fields_set:
            _dict['export'] = None

        # set to None if status (nullable) is None
        # and model_fields_set contains the field
        if self.status is None and "status" in self.model_fields_set:
            _dict['status'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Permissions from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "company": obj.get("company"),
            "send": obj.get("send"),
            "receive": obj.get("receive"),
            "webhook": obj.get("webhook"),
            "update": obj.get("update"),
            "log": obj.get("log"),
            "webhookhistory": obj.get("webhookhistory"),
            "export": obj.get("export"),
            "status": obj.get("status")
        })
        return _obj


